#!/usr/bin/env bash

set -e

DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )
ROOT_DIR="${DIR}/.."

(
  cd $ROOT_DIR/cdk
  ./script/ci
)

yarn install
yarn test
yarn build-storybook
yarn upload-artifact
